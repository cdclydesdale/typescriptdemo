{
  "name": "typescript-demo",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal TypeScript demo project",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
